{
  "build_type": {
    "default": "Debug",
    "description": "The CMake build type to use",
    "choices": {
      "Debug": {
        "short": "Debug",
        "long": "Disables optimizations and includes debug info.",
        "buildType": "Debug"
      },
      "Release": {
        "short": "Release",
        "long": "Includes optimizations but no debug info.",
        "buildType": "Release"
      }
    }
  },
  "log": {
    "default": "off",
    "description": "data-logging on/off",
    "choices": {
      "off": {
        "short": "log off",
        "long": "no data-logging",
        "settings": {
          "LOG": "false"
        }
      },
      "usb": {
        "short": "log to usb",
        "long": "with data-logging to usb",
        "settings": {
          "LOG": "USB"
        }
      },
      "serial": {
        "short": "log to serial",
        "long": "with data-logging to serial",
        "settings": {
          "LOG": "SERIAL"
        }
      }
    }
  }
}
